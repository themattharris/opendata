
    <div class="well">
    <% things.each do |i| %>
    <%= content_tag(:span, link_to(truncate(h(i.to_s), :length => 55, :omission => '...'), tag_path(i.slug)), :class => "label #{random_label_variant}") %>
    <% end %>
    </div>